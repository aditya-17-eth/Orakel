export function GET() { return Response.json({ ok: true, network: "stellar-testnet", service: "stellar-predict-web", contractConfigured: Boolean(process.env.NEXT_PUBLIC_CONTRACT_ID), pinataConfigured: Boolean(process.env.PINATA_JWT), supabaseConfigured: Boolean(process.env.SUPABASE_URL && process.env.SUPABASE_SERVICE_ROLE_KEY), keeperConfigured: Boolean(process.env.KEEPER_SECRET) }); }
